# LESSONS

## 待触发备忘

## 2026-08-30 — Local Diagnostics Foundation / 防护网基线

- [已结构化] 性能对比必须固定禁用状态、隔离 HOME、预热、样本量、算法和原始样本；基线已落入 T-001 证据，T-009 必须复用同一口径。
- [已结构化] BYZ 包测试直接消费 `dist` 时，证据必须绑定 HEAD 与 source/dist 内容哈希，不能只报告通过数量。
- [已结构化] 同一启动事件中的后续通知会覆盖前一条通知；一次性隐私提示必须在启动通知稳定后显示，并且只在实际显示时写 marker。
- [已结构化] `Worker.unref()` 必须放在生命周期监听器安装之后；添加监听器可能重新引用 MessagePort，导致本应立即退出的命令挂起。
